<h1><%= @category.name %></h1>
<% if @articles.each do |post| %>
	<h3><%= link_to article.title,post %></h3>
	<p><%= article.body %></p>
<% end.empty? %>
	<p>Sorry, there are no posts in this category</p>
<% end %>
	<%= link_to "Back to Categories",categories_path %>